#include "neural_net.h"
#include <stddef.h>

int main() {
  void * layer = NULL;
  int n = 2
  char fullyConnected[] = "fullyConnected";

  layer = addToNet(layer, NULL, &n);
  layer = addToNet(layer, NULL, &n);


  return 1;
}
